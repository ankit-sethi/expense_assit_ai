# Database
DB_USER=expense_user
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=expenses_db

# OpenAI
OPENAI_API_KEY=sk-...

# Telegram bot
TELEGRAM_BOT_TOKEN=your_bot_token

# SMS webhook — generate with:
# -join ((65..90) + (97..122) + (48..57) | Get-Random -Count 32 | % {[char]$_})
SMS_API_KEY=your-random-secret-key
